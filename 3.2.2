#include<iostream>
#include<string>
using std::cin;
using std::cout;
using std::endl;
using std::string;

int main()
{  
    //1.读string对象
    string s;
    cin>>s;
    cout << "我读到的s=" << s <<endl;
    
    
    string s1,s2;
    cin>>s1>>s2;
    cout << "读到的s1=" <<s1<< endl;
    cout << "读到的s2=" <<s2<< endl;
    
    
    //2.读取未知数量string对象
    string word;
    while(cin>>word)
        cout << word << endl;
     
    int n=0;
    while(cin>>n)   
       cout << "n" <<n<< endl;
    
    
    //3.使用gentlie读取一整行
    string line;
    while(getline(cin,line))
        cout << "我的一行" << line << endl;
    
    
    
    //4.empty.size操作
    string line;
    while(getline(cin,line))
    {
        if(line.empty())
            cout << "此行为空！" << endl;
        else
            cout << "此行不为空: " << line << endl;
    }
    
    


     string line;
    while(getline(cin,line))
    {
        if(line.size()<5)
            cout << "这行内容小于5个" << line << endl;
        else
            cout << "这行内容大于5个" << line << endl;
    }
    
    
    //两个string对象相加
    string s1="   i like you .";
    string s2="you don't know.";
    
    string s3=s1+s2;
    cout << "s3=" << s3 <<endl;
    
    s1+=s2;
    cout << "s1=" << s1 << endl;
    
   
    string s1,s2,s3;
    cin>>s1>>s2>>s3;
    
    string s4=s1+s2+s3;
    cout << "s4=" <<s4<< endl;
    
    string s5=s1+" "+s2+" "+s3;
    cout << "s5=" <<s5<< endl;

    return 0;
}
